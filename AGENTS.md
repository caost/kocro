# 저장소 작업 지침

## agent-harness 실행

- agent-harness가 전달한 작업 범위, 역할, 실행 단계와 승인된 계획을 따른다. 리뷰·진단 단계에서는 요청 없이 구현을 변경하지 않는다.
- 구현·검증 단계에서는 파일 수정, 로컬 빌드, 자동 테스트, 실패 원인 확인과 작업 범위 내 수정을 수행한다. 이 작업에 매번 별도 사용자 승인을 요청하지 않는다.
- 커밋·체크포인트는 하네스가 지정한 담당과 절차를 따른다. 다른 역할에 배정된 단계를 임의로 수행하지 않는다.
- 기존 사용자 변경을 보존하고 관련 없는 파일은 수정하지 않는다. `.harness/`의 실행 기록과 상태 DB를 직접 수정하거나 Git에 추가하지 않는다.

## 로컬 검증

- `xcodebuild test`, `swift test`, XCTest 등 해당 프로젝트의 자동 테스트와 빌드는 실행할 수 있다. 실패 후 수정에 따른 재실행에도 별도 승인은 필요하지 않다.
- 변경에 관련된 테스트부터 실행하고, 공통 동작에 영향을 주면 회귀 테스트 범위를 넓힌다. 관련 코드·환경이 바뀌지 않았고 검증 근거가 유효하면 같은 검사를 불필요하게 반복하지 않는다.
- 저장소 루트에서 사용하는 macOS 검증 명령은 다음과 같다.

```sh
xcodebuild test -project apps/macos/Kocro.xcodeproj -scheme Kocro -destination 'platform=macOS'
xcodebuild build -project apps/macos/Kocro.xcodeproj -scheme Kocro -configuration Release CODE_SIGN_IDENTITY=-
git diff --check
```

- 자동 테스트는 테스트용 데이터와 임시 경로를 사용한다. 실제 사용자 설정이나 실행 중인 앱의 데이터를 검증용으로 변경하지 않는다.
- 실제 앱에 키 입력을 보내거나 사용자 설정·시스템 권한·로그인 항목을 변경해야 하는 검증은 해당 작업까지 승인된 경우에 수행한다. 승인이 없으면 필요한 대상과 동작을 구체적으로 보고하고, 독립적으로 실행할 수 있는 자동 검증은 계속한다.

## 결과 보고

- 구현 반영과 검증 통과를 구분한다. 실행한 명령, 종료 코드, 검증한 커밋 또는 미커밋 변경, 실패·미실행 항목을 기록한다.
- 테스트나 빌드를 실행하지 않았으면 통과했다고 보고하지 않는다. 실패했으면 원인을 확인하고 담당 작업 범위에서 수정한 뒤 필요한 검증을 다시 수행한다.
- 하네스가 지정한 응답 형식과 상태값을 사용한다. 코드 수정 필요, 환경 문제, 사용자 승인 대기를 구체적인 사유로 구분한다.
- 이 문서가 허용한 로컬 테스트·빌드에 대해 승인 부재를 중단 사유로 보고하지 않는다. 실제로 추가 승인이 필요한 작업이 남았으면 완료한 검증과 남은 작업을 함께 보고한다.
