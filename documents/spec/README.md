# Spec

합의된 현행 요구사항의 정본을 관리한다.

## 어느 그룹인가

새 spec은 정책이나 기능을 소유하는 그룹 아래에 둔다. 공통 런타임 정책은 `runtime`, 운영체제별 입력 구현은 `platform`, 사용자 설정과 매크로 정의는 `configuration` 그룹을 우선 검토한다.

## 그룹 지도

아직 등록된 spec 그룹이 없다.
